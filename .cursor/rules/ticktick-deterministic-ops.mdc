---
description: 对确定性的滴答任务状态变更直接执行
alwaysApply: true
---

# 滴答确定性操作规则

当用户意图是明确且可执行的任务状态变更时，直接调用 `user-dida365` MCP 工具执行，而不是只给文字建议。

适用场景：
- 新建任务
- 完成任务
- 删除任务
- 更新任务标题/内容/状态等确定性属性

执行要求：
1. 调用 MCP 前必须先读取对应工具 schema（`mcps/user-dida365/tools/*.json`）。
2. 若输入信息已充分，直接执行；仅在缺少关键字段时才提问。
3. 若任务匹配不唯一，先搜索并让用户确认目标任务后再执行。
4. 执行后返回结果摘要（任务标题、任务 ID、状态、关键变更）。

默认行为：
- 用户说“完成这个任务”“把这个任务删掉”“新建一个任务……”等明确指令时，优先执行操作。
- 不擅自做超出用户指令范围的批量修改。
