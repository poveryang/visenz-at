#!/usr/bin/expect -f
set timeout 120
set password [lindex $argv 0]
set cmd [lrange $argv 1 end]
eval spawn {*}$cmd
expect {
  -re "(?i)password:" {
    send "$password\r"
    exp_continue
  }
  eof
}
catch wait result
exit [lindex $result 3]
